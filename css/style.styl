@import 'reset/reset.css'
@import '../bower_components/flex-grid/stylus/flexgrid'
@import '../bower_components/typographic/stylus/typographic'

$vertical-rhythm = false
$line-height-ratio = 1.25
$header-font-weight = 100
$header-font = 'proxima-nova', $helvetica
$body-font = 'proxima-nova', $helvetica
$min-font = 14px
$max-font = 16px
typographic()


h3
  line-height: 1.1em
  color: white

html
  overflow-y: hidden

body
  padding: 60px 15px 0


.masthead
  box()
  max-width: 980px
  margin: auto
  text-align: center
  div
    block(1/3)
    flex(masonry)
    padding: 30px
    background: dodgerblue
    color: white
    border-radius: 3px
    margin-bottom: 30px
    display: flex
    justify-content: center
    align-items: center
    &:first-child
      background: none
    p
      color: darken(dodgerblue, 50%)
      &:last-of-type
        margin-bottom: 0

.github
  img
    height: 45px

+below($min-width)
  .logo, .github
    order: -1


.grid
  padding: 30px 0
  div
    box()
    max-width: 980px
    margin: auto
    +below($min-width)
      box($gut: 0)
      padding: 0 15px
    &:nth-of-type(1)
      figure
        block(1/12)
        +below($min-width)
          block(1/12, $gut: 0)
    &:nth-of-type(2)
      figure
        block(1/11)
        +below($min-width)
          block(1/11, $gut: 0)
    &:nth-of-type(3)
      figure
        block(1/10)
        +below($min-width)
          block(1/10, $gut: 0)
    &:nth-of-type(4)
      figure
        block(1/9)
        +below($min-width)
          block(1/9, $gut: 0)
    &:nth-of-type(5)
      figure
        block(1/8)
        +below($min-width)
          block(1/8, $gut: 0)
    &:nth-of-type(6)
      figure
        block(1/7)
        +below($min-width)
          block(1/7, $gut: 0)
    &:nth-of-type(7)
      figure
        block(1/6)
        +below($min-width)
          block(1/6, $gut: 0)
    &:nth-of-type(8)
      figure
        block(1/5)
        +below($min-width)
          block(1/5, $gut: 0)
    &:nth-of-type(9)
      figure
        block(1/4)
        +below($min-width)
          block(1/4, $gut: 0)
    &:nth-of-type(10)
      figure
        block(1/3)
        +below($min-width)
          block(1/3, $gut: 0)
    &:nth-of-type(11)
      figure
        block(1/2)
        +below($min-width)
          block(1/2, $gut: 0)
    &:nth-of-type(12)
      figure
        block(1/1)
        +below($min-width)
          block(1/1, $gut: 0)
  figure
    background: salmon
    color: white
    font-weight: bold
    border-radius: 3px
    padding: 5px
    text-align: center
    margin-bottom: 30px
    +below($min-width)
      border-radius: 0
      font-weight: 100
      border-right: 1px solid darken(salmon, 5%)
      &:first-child
        border-radius: 3px 0 0 3px
      &:last-child
        border-radius: 0 3px 3px 0
        border-right: 0


.masonry
  margin-bottom: 30px
  div
    box()
    max-width: 980px
    margin: auto
  figure
    block(1/3)
    flex(masonry)
    margin-bottom: 30px
    padding: 10px
    background: springgreen
    border-radius: 3px
  img
    width: 100%
